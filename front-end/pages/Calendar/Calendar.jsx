import React from 'react';
import CustomCalendar from '../../components/CustomCalendar/CustomCalendar'; // Adjust the path if necessary

const Calendar = () => {
  return (
    <div>
      <h2>Booking Calendar</h2>
      <CustomCalendar />
    </div>
  );
};

export default Calendar;
